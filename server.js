const express = require('express'); //links in express from the Nodes Modules

const app = express();
const PORT = 5000;

app.listen(PORT);  //server needs to constantly be moving (request, receive, request, receive, etc.)
